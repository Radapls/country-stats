import { Injectable } from '@angular/core';

@Injectable()
export class CountriesAdapter
{
    // public countryAdapter(response: Array<Countries>): Array<CountriesResponse>
    // {
    //     return response.map((country: Countries) => ({
    //         name: country.name,
    //         currency: country.currencies,
    //         region: country.region,
    //         ubication: country.latLng,
    //         icon: country.iconFlag,
    //         area: country.area,
    //         flag: country.flags,
    //         escudo: country.coatOfArms
    //     }));
    // };
}